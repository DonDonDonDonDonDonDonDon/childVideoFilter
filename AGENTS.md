# 專案協作規範（AGENTS.md）

## 1. 專案定位與核心原則

本專案為 Chrome Manifest V3 擴充功能，目標是協助過濾或處理可能不適合兒少觀看的影音內容。所有實作、審查與文件更新，均應優先考量：

1. **兒少安全優先**：不得設計會降低保護效果、規避家長控管，或蒐集兒少不必要資料的功能。
2. **最小權限原則**：只申請實際功能所需的 Chrome 權限與網站比對範圍；新增權限前必須說明用途、影響範圍及替代方案。
3. **隱私預設保護**：不得在未取得明確同意下蒐集、上傳、追蹤或分享瀏覽紀錄、影音標題、網址、帳號資訊或可識別個人資料。
4. **可驗證性**：每項行為必須可透過明確程式流程、日誌或測試步驟驗證；避免隱式副作用與難以追溯的狀態。
5. **安全失敗（fail closed）**：內容判斷、通訊或設定讀取失敗時，應採取不暴露未經確認內容的保守策略，並以使用者可理解的方式告知狀態。

## 2. 溝通與文件語言

- 程式碼註解、README、提交訊息、介面文字與新增文件一律以**繁體中文**為主。
- 專有名詞、API 名稱與程式識別字可保留英文，例如 `chrome.scripting.executeScript`、`Content Security Policy`。
- 註解應解釋「原因、限制或風險」，不要逐字翻譯程式碼。
- 發現需求、資安、隱私或兒少保護面有歧義時，先記錄假設與風險；不得自行放寬安全或隱私限制。

### 2.1 功能說明必備內容

- 每個新增、修改或移除的功能，都必須提供適合使用者與維護者理解的繁體中文說明；不可只交付程式碼或只說「已完成」。
- 功能說明至少包含下列項目：
  1. **目的**：此功能解決什麼問題，以及適用的頁面或情境。
  2. **操作與結果**：使用者如何啟用、輸入或操作；成功後會看到什麼行為或介面變化。
  3. **實作概要**：涉及的主要檔案、資料來源、事件或程式流程；避免逐行重述程式碼。
  4. **資料、權限與隱私**：是否讀寫 `chrome.storage`、新增權限、處理頁面資料或對外傳輸資料。
  5. **限制與失敗行為**：可能無法涵蓋的網站版面、判斷依據、錯誤處理方式，以及安全預設行為。
  6. **驗證方式**：至少一組可重現的測試步驟與預期結果。
- 使用者介面中的開關、輸入欄位、按鈕、提示訊息與遮罩，皆須說明其用途、影響範圍及停用／復原後的行為。
- 若功能會改變兒少可觀看的內容，必須明確說明判斷依據（例如影片標題關鍵字）、可能的誤判或漏判，及其非取代家長監護工具的限制。
- 複雜或安全關鍵功能應在相關程式碼附近保留簡短註解，並在回報中提供上述完整摘要。

## 3. 現有架構與責任邊界

| 檔案 | 責任 | 不應承擔的責任 |
| --- | --- | --- |
| `manifest.json` | 擴充功能中繼資料、權限、注入設定與入口宣告 | 業務邏輯、機密資料 |
| `popup.html` | Popup 的結構與可存取語意 | 直接操作網頁 DOM、內嵌複雜腳本 |
| `popup.js` | Popup 事件、狀態顯示與向分頁發送訊息 | 直接存取頁面 DOM、長時間背景工作 |
| `execute.js` | Content script；限定於匹配頁面操作 DOM 與回應訊息 | 信任未驗證訊息、保存敏感資料 |

- 若功能需要長時間運行、集中狀態或跨分頁協調，新增 Manifest V3 service worker，並清楚定義其訊息合約。
- Popup、content script 與 service worker 屬不同執行環境；不可假設它們共享 JavaScript 變數、DOM 或生命週期。
- 任何跨環境通訊均應使用明確 `action`／資料結構，驗證來源、資料型別與允許值，並回傳成功或失敗結果。

## 4. 程式碼規範

- 使用現代 JavaScript；採用 `const` 優先、必要時才用 `let`，禁止 `var`。
- 使用 2 個空白字元縮排、分號、單引號；保持與現有程式一致的前提下逐步整理。
- DOM 查詢結果與非同步 API 回傳值必須檢查是否存在，避免頁面結構變更或分頁關閉時產生未捕捉例外。
- 非同步流程使用 `async`／`await`，並以 `try...catch` 處理可預期失敗；錯誤訊息不得包含敏感內容。
- 不得使用 `eval`、`new Function`、字串形式的 `setTimeout`／`setInterval`，也不得注入未受信任的 HTML。
- 操作頁面內容時以 `textContent`、DOM API 與嚴格白名單為優先；禁止將外部或頁面資料直接寫入 `innerHTML`。
- 複雜判斷應拆成具語意的純函式；避免單一事件處理器同時負責 UI、判斷、注入與錯誤處理。
- **程式碼註解規範（繁體中文）**：
  1. **每個函式/類別須有 JSDoc 風格註解**：說明用途、參數、回傳值、副作用、拋出錯誤情況。
  2. **複雜邏輯、演算法、正則表達式、非直觀處理**必須加上行內註解，解釋「為什麼這樣做」、「有何限制或風險」。
  3. **註解不得逐字翻譯程式碼**（如 `i++ // i 加 1`），應說明意圖與業務脈絡。
  4. **TODO/FIXME/NOTE** 等標記須包含具體說明與負責人/時程，禁止留空。
  5. **公開 API、訊息合約、storage 結構** 變更時，同步更新對應註解與文件。

## 5. 權限、隱私與資安要求

- 修改 `permissions`、`host_permissions`、`content_scripts.matches` 或動態注入目標時，必須在變更說明中列出：必要性、影響網站、是否可縮小範圍與測試結果。
- 優先使用 `activeTab` 與精確網址比對；不得以 `<all_urls>` 或廣泛萬用字元作為方便的預設。
- 禁止硬編碼 API 金鑰、Token、帳密、個人識別資訊或私有網址；若未來需要後端服務，憑證應由受控服務端保存。
- 不得傳送使用者瀏覽資料至外部服務，除非需求明確授權、資料最小化、傳輸加密且有清楚隱私告知。
- 所有來自網頁、儲存空間與訊息通道的資料皆視為不可信任輸入，必須驗證後才使用。
- 依 Chrome Web Store 與 Manifest V3 規範，禁止遠端載入或執行程式碼。

## 6. 變更流程

1. 先閱讀相關檔案、現有行為與未提交變更；未經明確要求，不覆寫或還原使用者的工作。
2. 以最小可行變更實作需求，避免混入無關重構或格式化。
3. 若修改訊息格式、權限或內容判斷邏輯，同步更新對應文件與手動驗證步驟。
4. 每次變更後至少執行語法或靜態檢查，並在 Chrome 的「擴充功能」頁重新載入後進行手動驗證。
5. 回報時列出修改檔案、驗證結果、尚未驗證項目與已知限制。

## 7. 最低驗證清單

- `manifest.json` 為有效 JSON，且宣告與實際檔案名稱一致。
- Popup 可正常開啟，互動元件能被正確取得，且按鈕事件只註冊一次。
- 在匹配與不匹配網站各測試一次：不匹配網站不得注入或拋出未處理錯誤。
- content script 接收到未知、缺少或格式錯誤的訊息時，不得改變頁面狀態。
- 重新載入擴充功能、重新整理分頁及切換分頁後，功能不應重複注入、重複綁定事件或殘留不一致狀態。
- 檢查瀏覽器 Console 與擴充功能 Service Worker Console，確認無未處理例外、機密資料輸出或權限警告。

## 8. 提交與審查標準

- 一個提交聚焦一項可描述的目的；提交訊息使用繁體中文，動詞開頭，例如：`新增：影片遮罩切換功能`。
- 審查時特別檢查：權限擴張、跨環境訊息驗證、DOM 注入、敏感資料、失敗時的保護行為，以及對兒少的潛在負面影響。
- 不得提交建置產物、瀏覽器設定檔、個人資料、金鑰或未經授權的第三方程式碼。
- 對安全、隱私或內容過濾判斷有不確定性時，將其標示為待確認事項，不得以猜測取代需求確認。
